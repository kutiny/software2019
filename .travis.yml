language: java
jdk: oraclejdk8
dist: trusty

before_install:
    - chmod +x gradlew
    - chmod +x after_script.sh

notifications:

    email:
        - alex08lta@gmail.com
        - juanfernandez@alumnos.unc.edu.ar

script:
    - ./gradlew clean build

after_script:
    - ./after_script.sh

git:
    quiet: true
